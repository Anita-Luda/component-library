/**
 * LIBRARY ORCHESTRATOR
 */
const Library = {
    atoms: window.Atoms,
    molecules: window.Molecules,
    organisms: window.Organisms,
    templates: window.Templates
};

window.Library = Library;
